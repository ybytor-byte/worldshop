{
  "build": {
    "builder": "NIXPACKS",
    "rootDirectory": "backend"
  },
  "deploy": {
    "startCommand": "cd backend && node node_modules/prisma/build/index.js db push --accept-data-loss && node dist/src/main.js",
    "healthcheckPath": "/docs",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
