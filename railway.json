{
  "build": {
    "builder": "NIXPACKS",
    "rootDirectory": "backend"
  },
  "deploy": {
    "startCommand": "cd backend && node node_modules/prisma/build/index.js migrate deploy && node dist/src/main.js",
    "healthcheckPath": "/docs",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
