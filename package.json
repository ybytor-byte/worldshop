{
  "name": "worldshop",
  "private": true,
  "scripts": {
    "build": "cd backend && npm ci --include=dev && npm run build",
    "start": "node backend/dist/src/main.js",
    "dev": "cd backend && npm run dev"
  }
}
